# General -----------------------------------------------------------------------------------------------

# Sources .bashrc
[[ -f ~/.bashrc ]] && . ~/.bashrc

# Starts window manager on boot
#if [ -z "$DISPLAY" -a $XDG_VTNR -eq 1 ]; then
#    startx
#fi

#--------------------------------------------------------------------------------------------------------
